# Auto-loaded by `docker compose up` for local development.
# Not used in production (where -f docker-compose.prod.yml is specified instead).
services:
  loom-clone-server:
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
