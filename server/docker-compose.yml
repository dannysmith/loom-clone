services:
  loom-clone-server:
    build: .
    container_name: loom-clone-server
    environment:
      - HOST=0.0.0.0
      - PORT=3000
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
