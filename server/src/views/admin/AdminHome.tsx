import { AdminLayout } from "../layouts/AdminLayout";

// Placeholder. Phase 6 (task-x5) replaces this with the real dashboard.
export function AdminHome() {
  return (
    <AdminLayout title="Admin · Loom Clone">
      <h1>Admin</h1>
      <p>Phase 6 (task-x5) lives here. Empty for now.</p>
    </AdminLayout>
  );
}
