# Ignore all contents of this directory — individual run logs can be
# large and are not worth tracking. The directory itself is tracked
# via .gitkeep so the default `test-runs-root` path always exists.
*
!.gitignore
!.gitkeep
