# Ignore all individual run directories — they contain logs, raw
# video outputs, and system snapshots that are too large (and too
# machine-specific) to track. Summary markdown files that record
# aggregate findings across a tier run are tracked.
*
!.gitignore
!.gitkeep
!*.md
